export class User {

  uid?: string;
  email?: string;
  displayName?: string;
  photoURL?: string;
  emailVerified?: boolean;

  // future state
  firstName?: string;
  lastName?: string;
  bio?: string;
  token?: string;
}
